INSERT INTO SALE_STATE
           (Description)
    SELECT 'Initiated'

	INSERT INTO SALE_STATE
           (Description)
    SELECT 'Reserved'

	INSERT INTO SALE_STATE
           (Description)
    SELECT 'Finalized'

	INSERT INTO SALE_STATE
           (Description)
    SELECT 'Returned'
